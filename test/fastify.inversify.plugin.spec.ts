import {describe, it, expect } from "@jest/globals"

describe('Inversify Plugin', () => {
  it('should provide container to requests', () => {
    expect(false).toBeTruthy()
  })
  it('should clean up request scoped container when response is sent', () => {
    expect(false).toBeTruthy()
  })
})